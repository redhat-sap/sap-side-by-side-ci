kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: app-be
  namespace: app-be
  labels:
    app: app-be
spec:
  to:
    kind: Service
    name: app-be
    weight: 100
  port:
    targetPort: 8080-tcp
  wildcardPolicy: None