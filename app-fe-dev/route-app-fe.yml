kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: app-fe
  namespace: app-fe-dev
  labels:
    app: app-fe
spec:
  host: app-fe-dev.apps.cluster-{{ guid }}.{{ ocp4_base_domain }}
  to:
    kind: Service
    name: app-fe
    weight: 100
  port:
    targetPort: 8080-tcp
  wildcardPolicy: None
